To start the project you need to run the Simfony server with the command:
symfony server:start
Then open the application at:
http://localhost:8000/
The map will load and a field will appear where you can enter addresses. after pressing the button, all possible choices will be displayed and when you click on some the map will be positioned on the place